{
  "dependencies": {
    "electron": "^37.3.0"
  },
  "name": "electronfile",
  "version": "1.0.0",
  "description": "This is the main folder for electron js",
  "main": "mainElectron.js",
  "devDependencies": {
    "concurrently": "^8.2.2",
    "cross-env": "^10.0.0",
    "wait-on": "^7.2.0"
  },
  "scripts": {
    "start": "electron .",
    "dev": "cross-env ELECTRON_DEV=1 electron .",
    "start-app": "concurrently \"cd ../ReactGui && npm run dev -- --port 5173 --strictPort\" \"wait-on http://localhost:5173 && npm run dev\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Deep Kayastha",
  "license": "ISC"
}
